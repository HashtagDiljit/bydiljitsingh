<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Wooden Bridge (IDP1) — By Diljit Singh</title>
  <link rel="stylesheet" href="../styles.css" />
</head>
<body>
<!-- ========== HEADER (SUBPAGE) ========== -->
<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="../">
      <img src="../assets/logo.svg" alt="by DS logo" width="28" height="28" />
      <span>bydiljitsingh</span>
    </a>
    <button class="hamburger" id="openDrawer" aria-label="Open menu" aria-controls="drawer" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </div>
</header>

<aside id="drawer" class="drawer" aria-hidden="true">
  <button class="drawer-close" id="closeDrawer" aria-label="Close menu">×</button>
  <nav class="drawer-nav" aria-label="Off-canvas">
    <a href="../projects/">Projects</a>
    <a href="../wiki/">My Wiki</a>
    <a href="../toolkit/">Toolkit</a>
    <a href="../cv/">CV</a>
    <a href="../#contact">Contact</a>
  </nav>
</aside>
<div id="backdrop" class="backdrop" hidden></div>
</header>

<main class="section container">
  <h1>Wooden Bridge (IDP1)</h1>
  <p class="section-sub">Cohort-best load test: <strong>2.4 kN</strong> (pass requirement: 1.1 kN)</p>

  <img src="../assets/bridge.jpg" alt="Bridge project" style="width:100%;height:auto;border-radius:16px;border:1px solid var(--grey-2)">

  <h2>Brief</h2>
  <p>Design and build a wooden bridge to meet a minimum load requirement with constraints on materials and dimensions.</p>

  <h2>Approach</h2>
  <ul>
    <li>Selected truss configuration to maximise stiffness-to-mass.</li>
    <li>Prioritised glue joints and load paths to reduce stress concentrations.</li>
    <li>Built and tested with a two-person team due to limited availability.</li>
  </ul>

  <h2>Results</h2>
  <ul>
    <li>Withstood <strong>2.4 kN</strong> — highest in cohort.</li>
    <li>Exceeded pass requirement (1.1 kN) with significant safety margin.</li>
  </ul>

  <h2>Lessons</h2>
  <ul>
    <li>Joint quality and geometry selection dominate performance in lightweight structures.</li>
    <li>Lean team ⇒ faster iteration and clearer ownership.</li>
  </ul>
</main>

<footer class="site-footer"><div class="container"><small>© <span id="y"></span> By Diljit Singh</small></div></footer>
<script>
  // Smooth scroll for same-page anchors only
  document.querySelectorAll('a[href^="#"]').forEach(a=>{
    a.addEventListener('click',e=>{
      const id=a.getAttribute('href').slice(1), el=document.getElementById(id);
      if(!el) return;
      e.preventDefault();
      const y=el.getBoundingClientRect().top + window.scrollY - 72;
      window.scrollTo({top:y,behavior:'smooth'});
    });
  });

  // Drawer controls
  const drawer = document.getElementById('drawer');
  const openBtn = document.getElementById('openDrawer');
  const closeBtn = document.getElementById('closeDrawer');
  const backdrop = document.getElementById('backdrop');

  function openDrawer(){
    drawer.classList.add('open'); backdrop.hidden = false;
    openBtn?.setAttribute('aria-expanded','true');
    drawer.setAttribute('aria-hidden','false');
    document.body.style.overflow='hidden';
  }
  function closeDrawer(){
    drawer.classList.remove('open'); backdrop.hidden = true;
    openBtn?.setAttribute('aria-expanded','false');
    drawer.setAttribute('aria-hidden','true');
    document.body.style.overflow='';
  }
  openBtn?.addEventListener('click', openDrawer);
  closeBtn?.addEventListener('click', closeDrawer);
  backdrop?.addEventListener('click', closeDrawer);
  window.addEventListener('keydown', e=>{ if(e.key==='Escape') closeDrawer() });

  // Portrait reveal (if present)
  window.addEventListener('load', ()=>{
    document.querySelectorAll('.reveal').forEach(el=> setTimeout(()=>el.classList.add('enter'),80));
    const y=document.getElementById('y'); if(y) y.textContent=new Date().getFullYear();
  });
</script>

</body>
</html>

